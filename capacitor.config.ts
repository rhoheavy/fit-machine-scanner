import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'fit-machine-scanner',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
